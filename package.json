{
  "name": "cybertron-translator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test ./tests/translator.test.mjs ./tests/recognizer.test.mjs",
    "test:browser": "node ./tests/browser-smoke.mjs",
    "check": "npm test && npm run build"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "vite-plugin-pwa": "^1.0.3"
  }
}